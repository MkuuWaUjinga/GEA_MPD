dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
script:
# set up local aws testing environment
# run tests
  - ./Backend/scripts/run_tests.sh
after_success:
  - ./Backend/scripts/deploy.sh